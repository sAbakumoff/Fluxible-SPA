'use strict';

var React = require('react');

var HomePage = React.createClass({
    render : function(){
        return (
          <strong>Here will be the home page, i.e. dashboard.</strong>
        );
    }
});

module.exports = HomePage;
